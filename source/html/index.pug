doctype html
html(lang="en", dir="ltr")
  head
    title SAFK - Social Media Agency

    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0, minimal-ui")
    meta(http-equiv="X-UA-Compatible", content="IE=edge")
    meta(name="keywords", content="social media, branding, user interfaces, motion graphics, agency, safk")

    link(type="image/x-icon", rel="icon", href="assets/images/favicon.ico")

    <!--[if lt IE 9]>
    script(src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js")
    script(src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js")
    <![endif]-->
    script.
      (function () {
        "use strict";

        var fontHref = "https://fonts.googleapis.com/css?family=Libre+Baskerville|Lato:300,400,700";

        if ((window.localStorage && localStorage.font_css_cache) || document.cookie.indexOf('font_css_cache') > -1) {
          injectFontsStylesheet();
        } else {
          window.addEventListener('load', injectFontsStylesheet);
        }

        function fileIsCached(href) {
          return window.localStorage && localStorage.font_css_cache && (localStorage.font_css_cache_file === href);
        }

        function injectFontsStylesheet() {
         // if this is an older browser
          if (!window.localStorage || !window.XMLHttpRequest) {
            var stylesheet = document.createElement('link');
            stylesheet.href = fontHref;
            stylesheet.rel = 'stylesheet';
            stylesheet.type = 'text/css';
            document.getElementsByTagName('head')[0].appendChild(stylesheet);
            // just use the native browser cache
            // this requires a good expires header on the server
            document.cookie = "font_css_cache";

          // if this isn't an old browser
          } else {
             // use the cached version if we already have it
            if (fileIsCached(fontHref)) {
              injectRawStyle(localStorage.font_css_cache);
            // otherwise, load it with ajax
            } else {
              var xhr = new XMLHttpRequest();
              xhr.open("GET", fontHref, true);
              // cater for IE8 which does not support addEventListener or attachEvent on XMLHttpRequest
              xhr.onreadystatechange = function () {
                if (xhr.readyState === 4) {
                  // once we have the content, quickly inject the css rules
                  injectRawStyle(xhr.responseText);
                  // and cache the text content for further use
                  // notice that this overwrites anything that might have already been previously cached
                  localStorage.font_css_cache = xhr.responseText;
                  localStorage.font_css_cache_file = fontHref;
                }
              };
              xhr.send();
            }
          }
        }
        // this is the simple utitily that injects the cached or loaded css text
        function injectRawStyle(text) {
          var style = document.createElement('style');
          // cater for IE8 which doesn't support style.innerHTML
          style.setAttribute("type", "text/css");
          if (style.styleSheet) {
              style.styleSheet.cssText = text;
          } else {
              style.innerHTML = text;
          }
          document.getElementsByTagName('head')[0].appendChild(style);
        }
      }());

    link(rel="stylesheet", media="all", href="/assets/stylesheet/main.css")

    script(src="/assets/javascript/bundle.js")
  body
    section.intro.cd-section.visible
      div
        ul.genertiswinner.genertiswinner--white
          li.active Who we are
          li What we do
          li Say Hello
        .intro__image
          .intro__tilt
            img.intro__tilt__image.tilt-effect(src="/assets/images/background1.jpg", data-tilt-options='{ "opacity" : 0.7, "movement": { "perspective" : 1000, "translateX" : 70, "translateY" : 10, "translateZ" : 2, "rotateX" : 3, "rotateY" : 3 } }')
        .container.intro__container
          h1.intro__title We are SAFK
          h2.intro__description Social Media Agency with a growing family of dreamers and builders
        
    section#about.about.cd-section
      div(
        data-vide-bg="mp4: /assets/videos/fire, webm: /assets/videos/fire, ogv: /assets/videos/fire, poster: /assets/images/fire",
        data-vide-options="muted: true"
      )
        ul.genertiswinner.genertiswinner--white
            li Who we are
            li.active What we do
            li Say Hello
        .container.about__container
          .about__block
            h4.about__block__title About Us
            h3.about__block__headline We Love to Create & Inspire Others
            p.about__block__text High-end camera does not make you a photographer. In the same fashion, neither does a good MacBook make you a designer. It is what you do which makes you awesome. That is why we believe that in simplicity lies power. So let us to help you think different.
            p.about__block__text While walking around the street or surfing in internet you will see roughly 3000 brands every day! From that number you will only knowledge only 1% of them. We are here to help your company and brand be in the 1%! We do something which is powerful, simple and mindblowing.
          .about__block
            h4.about__block__title Our Services
            ul.about__block__list
              li Brand Identities
              li User Interfaces
              li Motion Graphics
              li Marketing
              li Social Media

    section#contact.contact.cd-section
      div
        ul.genertiswinner
          li Who we are
          li What we do
          li.active Say Hello
        .contact__centre
          .contact__text
            h3 Have A Project To Discuss?
            a(href="mailto:hi@safk.co?subject=Hi") Let's Talk.
        .container.contact__info
          .contact__info__address
            span.contact__info__address--email hi@safk.co
            .contact__info__address--divider
          ul.contact__info__social
            li
              a(href="http://fb.me/safkagency") Check us out @Facebook

    script(type='text/javascript').
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

      ga('create', 'UA-89630629-1', 'auto');
      ga('send', 'pageview');
